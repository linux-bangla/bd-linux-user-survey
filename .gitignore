# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
tfplan

# Secrets — never commit plaintext, only the .enc version
infra/production.secrets.tfvars

# Age key — never commit
age.key
